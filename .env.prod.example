# ============================================
# Production Environment Variables
# Copy to .env.prod and fill in real values
# ============================================

# Server public IP (used for MinIO public URL)
SERVER_IP=80.87.102.197

# Database
DB_USER=travelio
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_NAME=travelio

# JWT
JWT_SECRET=CHANGE_ME_VERY_LONG_RANDOM_STRING_AT_LEAST_64_CHARS

# MinIO credentials
MINIO_ROOT_USER=travelio-admin
MINIO_ROOT_PASSWORD=CHANGE_ME_MINIO_PASSWORD_MIN_8_CHARS
MINIO_BUCKET=travelio
